# WP-A 安全分类闭环 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复两个真实安全洞——Plugin 权限门禁在 activate 前生效（P1#2），MCP 工具按 server annotations 分级风险（P1#5）。

**Architecture:** 就地最小改动。Plugin 侧在 `sandbox.py` 引入默认权限集与 compat/strict 模式、移除 legacy fail-open，并在 `manager.py` 将权限预检前置到 activate 之前；MCP 侧在 `tool_adapter.py` 解析 annotations 计算 risk_level 并重写 execution_mode。配套新增一个 config 项，默认值保证老用户零感知。

**Tech Stack:** Python 3.11、Pydantic（config schema）、pytest、loguru、ruff。

**Spec:** `docs/superpowers/specs/2026-06-15-wp-a-安全分类闭环-design.md`

---

## 文件结构

| 文件 | 职责 | 操作 |
|---|---|---|
| `echo_agent/config/schema.py:587` | `PluginsConfig` 新增 `permission_mode` 字段 | Modify |
| `echo_agent/plugins/sandbox.py` | 默认权限集常量、`mode` 参数、`_effective_permissions`、移除 fail-open | Modify |
| `echo_agent/plugins/manager.py:119-191` | `_load_and_activate` 权限预检前置 | Modify |
| `echo_agent/mcp/tool_adapter.py` | annotations → risk_level；重写 `execution_mode` | Modify |
| `tests/test_plugin_sandbox.py` | 更新 legacy 用例、新增 compat/strict/默认集用例 | Modify |
| `tests/test_plugin_manager.py` | 新增"strict 越权拒绝且 activate 未调用"用例 | Modify |
| `tests/test_mcp_security.py` | 新增 MCP 风险分类用例 | Modify |

依赖顺序：Task 1（config）→ Task 2（sandbox）→ Task 3（manager）为 Plugin 链；Task 4（MCP）独立。建议按编号执行。

---

## Task 1: config 新增 permission_mode 字段

**Files:**
- Modify: `echo_agent/config/schema.py:587-595`
- Test: `tests/test_plugin_sandbox.py`（借用，验证默认值）

- [ ] **Step 1: 写失败测试**

在 `tests/test_plugin_sandbox.py` 末尾追加：

```python
def test_plugins_config_default_permission_mode():
    from echo_agent.config.schema import PluginsConfig

    cfg = PluginsConfig()
    assert cfg.permission_mode == "compat"


def test_plugins_config_accepts_strict_mode():
    from echo_agent.config.schema import PluginsConfig

    cfg = PluginsConfig(permission_mode="strict")
    assert cfg.permission_mode == "strict"
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pytest tests/test_plugin_sandbox.py::test_plugins_config_default_permission_mode -v`
Expected: FAIL — `AttributeError` 或 pydantic 校验错误（字段不存在）

- [ ] **Step 3: 加字段**

`echo_agent/config/schema.py`，确认文件顶部已 `from typing import Literal`（schema.py 已在用 Literal，无需新增 import）。在 `PluginsConfig` 中 `trusted_plugins` 后追加：

```python
class PluginsConfig(_Base):
    """Plugin system configuration."""

    enabled: bool = True
    allow: list[str] = Field(default_factory=list)
    deny: list[str] = Field(default_factory=list)
    extra_dirs: list[str] = Field(default_factory=list)
    config: dict[str, dict[str, Any]] = Field(default_factory=dict)
    trusted_plugins: list[str] = Field(default_factory=list)
    permission_mode: Literal["compat", "strict"] = "compat"
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pytest tests/test_plugin_sandbox.py::test_plugins_config_default_permission_mode tests/test_plugin_sandbox.py::test_plugins_config_accepts_strict_mode -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add echo_agent/config/schema.py tests/test_plugin_sandbox.py
git commit -m "PluginsConfig 新增 permission_mode 配置项，默认 compat"
```

---

## Task 2: sandbox 引入默认权限集与 compat/strict 模式

**Files:**
- Modify: `echo_agent/plugins/sandbox.py`
- Test: `tests/test_plugin_sandbox.py`

- [ ] **Step 1: 更新现有 legacy 用例 + 写新失败测试**

现有 `test_legacy_plugin_warns_but_allows` 断言 legacy 能 `check_network()`，这正是要废除的 fail-open。先把它改成反映新行为，再加新用例。

将 `tests/test_plugin_sandbox.py` 中的 `test_legacy_plugin_warns_but_allows` 整体替换为：

```python
    def test_legacy_plugin_compat_allows_only_default_set(self):
        manifest = _make_manifest([])
        sandbox = PluginSandbox("test", manifest, trusted=False, mode="compat")
        assert sandbox.is_legacy is True
        # 默认集：tool.register / hook.register 放行
        assert sandbox.check_tool_register() is True
        assert sandbox.check_hook_register() is True
        # 高风险权限不在默认集，拦截
        assert sandbox.check_network() is False
        assert sandbox.check_subprocess() is False
        assert sandbox.check_filesystem_write() is False

    def test_legacy_plugin_strict_allows_nothing(self):
        manifest = _make_manifest([])
        sandbox = PluginSandbox("test", manifest, trusted=False, mode="strict")
        assert sandbox.is_legacy is True
        assert sandbox.check_tool_register() is False
        assert sandbox.check_hook_register() is False
        assert sandbox.check_network() is False

    def test_declared_permissions_unaffected_by_mode(self):
        # 显式声明权限的插件，行为不随 mode 变化
        manifest = _make_manifest(["network"])
        compat = PluginSandbox("t", manifest, trusted=False, mode="compat")
        strict = PluginSandbox("t", manifest, trusted=False, mode="strict")
        assert compat.check_network() is True
        assert strict.check_network() is True
        # 声明了 network 但没声明 tool.register → 仍拦截
        assert compat.check_tool_register() is False
        assert strict.check_tool_register() is False

    def test_trusted_bypasses_mode(self):
        manifest = _make_manifest([])
        sandbox = PluginSandbox("t", manifest, trusted=True, mode="strict")
        assert sandbox.check_network() is True
        assert sandbox.check_tool_register() is True
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pytest tests/test_plugin_sandbox.py -v`
Expected: FAIL — `PluginSandbox.__init__` 不接受 `mode` 关键字参数（`TypeError`）

- [ ] **Step 3: 改写 sandbox.py**

将 `echo_agent/plugins/sandbox.py` 的常量区与 `__init__`、`check_permission` 替换为以下内容（其余 `check_*` 包装方法不变）：

```python
VALID_PERMISSIONS = frozenset({
    "filesystem.read",
    "filesystem.write",
    "network",
    "subprocess",
    "tool.register",
    "hook.register",
})

# legacy（未声明 permissions）插件在 compat 模式下获得的最小默认权限集。
# 仅含最常见、最低风险的注册类权限；network/subprocess/filesystem.* 须显式声明。
DEFAULT_LEGACY_PERMISSIONS = frozenset({
    "tool.register",
    "hook.register",
})


class PluginSandbox:
    """Enforces declared permissions for a plugin."""

    def __init__(
        self,
        plugin_name: str,
        manifest: "PluginManifest",
        *,
        trusted: bool = False,
        mode: str = "compat",
    ):
        self._plugin_name = plugin_name
        self._trusted = trusted
        self._mode = mode
        self._is_legacy = len(manifest.permissions) == 0
        self._violations: list[str] = []

        if self._is_legacy:
            # compat：赋默认集；strict：空集（等价于拒绝一切未声明操作）
            self._effective_permissions = (
                set(DEFAULT_LEGACY_PERMISSIONS) if mode == "compat" else set()
            )
        else:
            self._effective_permissions = set(manifest.permissions)

        if self._is_legacy and not trusted:
            logger.warning(
                "Plugin '{}' has no permissions declared (legacy mode, "
                "permission_mode={}) — consider adding a 'permissions' field "
                "to its manifest",
                plugin_name,
                mode,
            )
```

`check_permission` 替换为（移除 `is_legacy → return True` 分支）：

```python
    def check_permission(self, required: str) -> bool:
        """Check if the plugin has the required permission.

        Returns True if allowed, False if denied.
        Trusted plugins always pass. Legacy plugins are governed by
        permission_mode (compat: default set; strict: nothing).
        """
        if self._trusted:
            return True

        if required in self._effective_permissions:
            return True

        self._violations.append(required)
        logger.warning(
            "Plugin '{}' attempted undeclared operation '{}' — blocked. "
            "Declare it in manifest permissions to allow.",
            self._plugin_name,
            required,
        )
        return False
```

注意：`is_legacy` / `violations` property 与各 `check_*` 包装方法保持不变。

- [ ] **Step 4: 运行测试确认通过**

Run: `pytest tests/test_plugin_sandbox.py -v`
Expected: PASS（含 Task 1 的两条 config 用例）

- [ ] **Step 5: 提交**

```bash
git add echo_agent/plugins/sandbox.py tests/test_plugin_sandbox.py
git commit -m "Plugin 权限改为默认集+compat/strict 模式，移除 legacy fail-open"
```

---

## Task 3: manager 将权限预检前置到 activate 之前

**Files:**
- Modify: `echo_agent/plugins/manager.py:144-187`
- Test: `tests/test_plugin_manager.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_plugin_manager.py` 末尾追加。该测试构造一个声明了越权 provides（注册 tool）但 strict 模式空权限的插件，断言 `activate` **从未被调用**。

```python
def test_strict_mode_rejects_before_activate(tmp_path, monkeypatch):
    """strict 模式下 legacy 插件越权时，activate 不应被调用。"""
    import asyncio
    from echo_agent.config.schema import Config
    from echo_agent.plugins.manager import PluginManager
    from echo_agent.plugins.manifest import PluginManifest, PluginRecord, PluginProvides
    from echo_agent.agent.tools.registry import ToolRegistry
    from echo_agent.bus.queue import MessageBus

    activate_called = {"flag": False}

    def fake_activate(ctx):
        activate_called["flag"] = True

    # legacy（无 permissions）但 provides 声明注册 tool
    manifest = PluginManifest(
        name="evil",
        permissions=[],
        provides=PluginProvides(tools=["evil_tool"]),
    )
    record = PluginRecord(manifest=manifest, source="user")

    cfg = Config()
    cfg.plugins.permission_mode = "strict"

    mgr = PluginManager(
        config=cfg,
        workspace=tmp_path,
        bus=MessageBus(),
        tool_registry=ToolRegistry(),
    )

    monkeypatch.setattr(
        "echo_agent.plugins.manager.load_plugin_module",
        lambda rec: {"activate": fake_activate, "deactivate": None},
    )

    asyncio.run(mgr._load_and_activate(record))

    assert activate_called["flag"] is False
    assert record.status == "failed"
    assert "permission" in record.error.lower()
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pytest tests/test_plugin_manager.py::test_strict_mode_rejects_before_activate -v`
Expected: FAIL — 当前实现会先调 activate，`activate_called["flag"]` 为 True

- [ ] **Step 3: 改写 `_load_and_activate`**

`echo_agent/plugins/manager.py`，将第 144 行起到 activate 调用之间的逻辑改为：构造 sandbox 时传入 mode，并在 activate **之前**做预检。把 146 行替换并在 ctx 构造后、activate 调用前插入预检块。

将 `manager.py:144-146` 替换为：

```python
        trusted_list = getattr(self._config.plugins, "trusted_plugins", []) or []
        is_trusted = name in trusted_list
        mode = getattr(self._config.plugins, "permission_mode", "compat")
        sandbox = PluginSandbox(name, record.manifest, trusted=is_trusted, mode=mode)
```

在 ctx 构造之后（原 157 行 `)` 之后）、`activate_fn = interface["activate"]` 之前，插入 activate 前预检：

```python
        # activate 前权限预检：插件 manifest 声明的 provides 须在授权范围内。
        # 越权时 strict 拒绝加载（不调 activate），compat 记 warning 并继续
        # （事后核验仍会裁剪实际越权注册）。
        denied: list[str] = []
        if record.manifest.provides.tools and not sandbox.check_tool_register():
            denied.append("tool.register")
        if record.manifest.provides.hooks and not sandbox.check_hook_register():
            denied.append("hook.register")

        if denied and mode == "strict":
            record.status = "failed"
            record.error = f"permission denied for: {', '.join(denied)}"
            logger.warning(
                "Plugin '{}' rejected before activate — missing permissions: {}",
                name,
                ", ".join(denied),
            )
            return
```

注意：`record.manifest.provides` 是 `PluginProvides`，其 `.tools` / `.hooks` 为 list（见 `manifest.py`）。activate 之后的事后核验块（原 174-183 行）保持不变——compat 模式仍靠它裁剪实际越权注册。

- [ ] **Step 4: 运行测试确认通过**

Run: `pytest tests/test_plugin_manager.py::test_strict_mode_rejects_before_activate -v`
Expected: PASS

- [ ] **Step 5: 运行 manager 全量回归**

Run: `pytest tests/test_plugin_manager.py -v`
Expected: PASS（所有现有用例无回归）

- [ ] **Step 6: 提交**

```bash
git add echo_agent/plugins/manager.py tests/test_plugin_manager.py
git commit -m "Plugin 权限预检前置到 activate 之前，strict 越权拒绝加载"
```

---

## Task 4: MCP 工具按 annotations 分级风险

**Files:**
- Modify: `echo_agent/mcp/tool_adapter.py`
- Test: `tests/test_mcp_security.py`

- [ ] **Step 1: 写失败测试**

在 `tests/test_mcp_security.py` 末尾追加。注意 `MCPToolAdapter` 需要一个 client，构造时传 `None` 即可（这些用例不调 execute）。

```python
def test_mcp_adapter_readonly_hint_maps_read_only() -> None:
    from echo_agent.mcp.tool_adapter import MCPToolAdapter

    tool = {"name": "get", "description": "d", "annotations": {"readOnlyHint": True}}
    adapter = MCPToolAdapter("srv", tool, client=None)
    assert adapter.risk_level == "read_only"
    assert adapter.execution_mode({}) == "read_only"


def test_mcp_adapter_destructive_hint_maps_exec() -> None:
    from echo_agent.mcp.tool_adapter import MCPToolAdapter

    tool = {"name": "rm", "description": "d", "annotations": {"destructiveHint": True}}
    adapter = MCPToolAdapter("srv", tool, client=None)
    assert adapter.risk_level == "exec"
    assert adapter.execution_mode({}) == "side_effect"


def test_mcp_adapter_no_annotations_defaults_write() -> None:
    from echo_agent.mcp.tool_adapter import MCPToolAdapter

    adapter = MCPToolAdapter("srv", {"name": "x", "description": "d"}, client=None)
    assert adapter.risk_level == "write"
    assert adapter.execution_mode({}) == "side_effect"


def test_mcp_adapter_conflicting_hints_prefer_exec() -> None:
    from echo_agent.mcp.tool_adapter import MCPToolAdapter

    tool = {"name": "x", "description": "d",
            "annotations": {"readOnlyHint": True, "destructiveHint": True}}
    adapter = MCPToolAdapter("srv", tool, client=None)
    assert adapter.risk_level == "exec"


def test_mcp_adapter_malformed_annotations_default_write() -> None:
    from echo_agent.mcp.tool_adapter import MCPToolAdapter

    # annotations 非 dict
    a1 = MCPToolAdapter("srv", {"name": "x", "annotations": "nope"}, client=None)
    assert a1.risk_level == "write"
    # 字段非 bool
    a2 = MCPToolAdapter("srv", {"name": "x", "annotations": {"readOnlyHint": "yes"}}, client=None)
    assert a2.risk_level == "write"


def test_mcp_destructive_tool_reaches_exec_via_classifier() -> None:
    """端到端：destructive MCP 工具经 classify_risk 得到 EXEC（证明进审批面）。"""
    from echo_agent.mcp.tool_adapter import MCPToolAdapter
    from echo_agent.security.risk_classifier import classify_risk, RiskLevel

    tool = {"name": "drop", "description": "d", "annotations": {"destructiveHint": True}}
    adapter = MCPToolAdapter("srv", tool, client=None)
    risk = classify_risk(adapter.name, {}, tool_risk_level=adapter.risk_level)
    assert risk == RiskLevel.EXEC
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pytest tests/test_mcp_security.py -k mcp_adapter -v`
Expected: FAIL — `adapter.risk_level` 不存在（`AttributeError`），且 `execution_mode` 恒为 side_effect

- [ ] **Step 3: 改写 tool_adapter.py**

`echo_agent/mcp/tool_adapter.py`。在文件顶部 import 区追加：

```python
from echo_agent.security.risk_classifier import RiskLevel
```

在 `__init__`（设置 `self.parameters` 之后）追加 risk_level 计算：

```python
        self.risk_level = self._classify_risk(mcp_tool).value
```

新增私有静态方法（放在 `__init__` 之后、`execute` 之前）：

```python
    @staticmethod
    def _classify_risk(mcp_tool: dict[str, Any]) -> RiskLevel:
        """按 MCP annotations 保守分级：降级受限、升级顺从。

        destructiveHint → EXEC；readOnlyHint 且非 destructive → READ_ONLY；
        无 hint / 字段矛盾 / 解析异常 → WRITE（绝不因外部输入放松）。
        """
        annotations = mcp_tool.get("annotations")
        if not isinstance(annotations, dict):
            return RiskLevel.WRITE

        destructive = annotations.get("destructiveHint") is True
        read_only = annotations.get("readOnlyHint") is True

        if destructive:
            return RiskLevel.EXEC
        if read_only and not destructive:
            return RiskLevel.READ_ONLY
        return RiskLevel.WRITE
```

将原 `execution_mode`（72-73 行）替换为：

```python
    def execution_mode(self, params: dict[str, Any]) -> str:
        return "read_only" if self.risk_level == "read_only" else "side_effect"
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pytest tests/test_mcp_security.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add echo_agent/mcp/tool_adapter.py tests/test_mcp_security.py
git commit -m "MCP 工具按 annotations 分级风险等级，修正幂等分类"
```

---

## Task 5: 全量回归与 lint

**Files:** 无改动，仅验证。

- [ ] **Step 1: 全量测试**

Run: `pytest -q`
Expected: 全绿，无回归。若有失败，定位是否本计划改动引入——重点检查 `test_plugin_sandbox.py`、`test_plugin_manager.py`、`test_plugin_context.py`、`test_mcp_*`、审批相关测试。

- [ ] **Step 2: lint**

Run: `ruff check .`
Expected: 无错误。若有，按提示修复（常见为 import 顺序）。

- [ ] **Step 3: 若 Step 1/2 有修复，提交**

```bash
git add -A
git commit -m "WP-A 回归修复与 lint"
```

（若无修复则跳过本步。）

---

## 验证完成定义（对照 spec 第五节）

- [ ] Plugin：compat 下 legacy 只注册 tool/hook 通过；高风险被拦
- [ ] Plugin：strict 下 legacy 越权拒绝加载且 activate 未被调用
- [ ] Plugin：声明完整权限放行；trusted 直通；事后核验保留
- [ ] MCP：readOnly→read_only、destructive→exec、无 hint→write、矛盾→exec、畸形→write
- [ ] MCP：destructive 工具经 classify_risk 得 EXEC（进审批面）
- [ ] 全量 pytest 无回归；ruff 通过
- [ ] config 新增项默认 compat，老用户零感知

---

## 自查记录

- **Spec 覆盖**：P1#2 → Task 1/2/3；P1#5 → Task 4；测试与回归 → 各 Task 的测试步 + Task 5。spec 排除项（物理沙箱、白名单、改名）未进任何 Task，符合范围。
- **占位符扫描**：无 TBD/TODO；每个改码步均含完整代码。
- **类型一致性**：`PluginSandbox(mode=...)` 在 sandbox.py 定义、manager.py 与测试中调用一致；`risk_level`（str）在 adapter 设置、测试与 classify_risk 中读取一致；`_classify_risk` 返回 `RiskLevel`、`.value` 转 str 与 base.py 的 `risk_level: str` 约定一致。
- **已知现有用例更新**：`test_legacy_plugin_warns_but_allows` 在 Task 2 被替换（原断言 legacy 可 check_network，与新行为冲突）。
