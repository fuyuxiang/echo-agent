# WP-B 语义评测 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增基于 LLM-as-judge 的 `semantic_quality` 评测指标，让 Evolution 的晋升门禁能判断语义质量（P1 #3）。

**Architecture:** 新建 `evaluation/semantic_metrics.py` 承载 async LLM 调用（保持 `metrics.py` 纯函数不变）；`EvalRunner` 新增可选 `provider` 参数，在 case 有 `expected_output` 且 provider 可用时追加语义指标，自然计入 `avg_score`；`provider` 经 app.py 的 EvalRunner 工厂闭包注入（无需改 gate.py）；补充 baseline.yaml 数据让指标实际激活。

**Tech Stack:** Python 3.11、asyncio、pytest（含 pytest-asyncio）、ruff、`LLMProvider.chat_with_retry`。

**Spec:** `docs/superpowers/specs/2026-06-15-wp-b-语义评测-design.md`

---

## 与 spec 的一处偏差说明

Spec 3.2 / 第五节列出要改 `gate.py`（PromotionGate 透传 provider）。读代码后发现更简方案：`EvalRunner` 工厂 `_make_eval_runner`（app.py:161-166）是闭包，`provider` 已在其作用域内；`PromotionGate` 仅调用 `self._make_runner()`。因此只需在工厂内给 `EvalRunner(...)` 加 `provider=provider`，闭包自动捕获，**gate.py 与 EvolutionEngine 无需改动**。spec 意图（EvalRunner 拿到 provider）完全满足，机制更简。本计划据此执行，文件清单相应少一个 gate.py。

---

## 文件结构

| 文件 | 职责 | 操作 |
|---|---|---|
| `echo_agent/evaluation/semantic_metrics.py` | LLM-as-judge 的 `semantic_quality` async 函数 + prompt | Create |
| `echo_agent/evaluation/runner.py` | `EvalRunner` 新增 provider/judge_model 参数，追加语义指标 | Modify |
| `echo_agent/app.py:162` | EvalRunner 工厂注入 provider | Modify |
| `data/eval/baseline.yaml` | 3 个 case 补 expected_output | Modify |
| `tests/test_eval_semantic.py` | semantic_quality 单元测试 | Create |
| `tests/test_eval_runner.py` | runner 语义指标接线测试 | Create |

依赖顺序：Task 1（semantic_metrics）→ Task 2（runner）→ Task 3（app 接线 + 数据）→ Task 4（回归）。

参考事实（已核实）：
- `MetricResult`（metrics.py:9-14）：`name: str=""`, `score: float=0.0`, `passed: bool=False`, `details: dict=field(default_factory=dict)`
- `LLMProvider.chat_with_retry(**kwargs) -> LLMResponse`；`chat` 接受 `messages, tools=None, model=None, ...`；`LLMResponse.content` 为字符串
- runner.py:114-119 现有打分块；run_case 是 `async`

---

## Task 1: semantic_metrics.py（LLM-as-judge 指标）

**Files:**
- Create: `echo_agent/evaluation/semantic_metrics.py`
- Test: `tests/test_eval_semantic.py`

- [ ] **Step 1: 写失败测试**

创建 `tests/test_eval_semantic.py`：

```python
"""Tests for LLM-as-judge semantic_quality metric."""
from __future__ import annotations

import pytest

from echo_agent.evaluation.semantic_metrics import semantic_quality


class _FakeProvider:
    """Minimal stub: returns a preset content or raises."""
    def __init__(self, content: str | None = None, exc: Exception | None = None):
        self._content = content
        self._exc = exc
        self.calls = 0

    async def chat_with_retry(self, **kwargs):
        self.calls += 1
        if self._exc is not None:
            raise self._exc
        class _Resp:
            content = self._content
        return _Resp()


@pytest.mark.asyncio
async def test_semantic_quality_high_score_passes():
    provider = _FakeProvider(content='{"score": 0.9, "reasoning": "equivalent"}')
    result = await semantic_quality("the capital is Paris", "Paris is the capital", provider)
    assert result.name == "semantic_quality"
    assert result.score == 0.9
    assert result.passed is True


@pytest.mark.asyncio
async def test_semantic_quality_low_score_fails():
    provider = _FakeProvider(content='{"score": 0.6, "reasoning": "partial"}')
    result = await semantic_quality("exp", "act", provider)
    assert result.score == 0.6
    assert result.passed is False


@pytest.mark.asyncio
async def test_semantic_quality_unparseable_is_neutral():
    provider = _FakeProvider(content="looks good to me")
    result = await semantic_quality("exp", "act", provider)
    assert result.score == 0.5
    assert result.passed is False
    assert "raw" in result.details


@pytest.mark.asyncio
async def test_semantic_quality_exception_is_neutral():
    provider = _FakeProvider(exc=RuntimeError("timeout"))
    result = await semantic_quality("exp", "act", provider)
    assert result.score == 0.5
    assert result.passed is False
    assert "error" in result.details


@pytest.mark.asyncio
async def test_semantic_quality_clamps_out_of_range():
    provider = _FakeProvider(content='{"score": 1.5, "reasoning": "x"}')
    result = await semantic_quality("exp", "act", provider)
    assert result.score == 1.0
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/test_eval_semantic.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'echo_agent.evaluation.semantic_metrics'`

- [ ] **Step 3: 写实现**

创建 `echo_agent/evaluation/semantic_metrics.py`：

```python
"""Semantic evaluation metrics — LLM-as-judge scoring (I/O-bound).

Kept separate from metrics.py (pure synchronous functions) so that
async/LLM dependencies do not leak into the pure-function module.
"""
from __future__ import annotations

import json
from typing import TYPE_CHECKING

from loguru import logger

from echo_agent.evaluation.metrics import MetricResult

if TYPE_CHECKING:
    from echo_agent.models.provider import LLMProvider

_PASS_THRESHOLD = 0.7
_NEUTRAL_SCORE = 0.5

_SYSTEM_PROMPT = (
    "You are a judge that evaluates the quality of an AI assistant's reply. "
    "Focus only on semantic equivalence to the reference answer, not on wording. "
    "Respond with a single JSON object: {\"score\": <float 0.0-1.0>, "
    "\"reasoning\": \"<one short sentence>\"}. "
    "1.0 = semantically equivalent; 0.5 = partially relevant; 0.0 = unrelated or wrong."
)


def _build_user_prompt(expected: str, actual: str) -> str:
    return (
        f"Reference answer:\n{expected}\n\n"
        f"Assistant reply:\n{actual}\n\n"
        "Return the JSON object now."
    )


def _parse_score(content: str) -> float:
    """Extract a float score from the model's JSON reply. Raises on failure."""
    data = json.loads(content)
    return float(data["score"])


async def semantic_quality(
    expected: str,
    actual: str,
    provider: "LLMProvider",
    *,
    model: str | None = None,
) -> MetricResult:
    """Score how well `actual` matches `expected` in meaning, via LLM-as-judge.

    Failures (bad JSON, exceptions) return a neutral 0.5 so a flaky judge
    neither rewards nor penalizes a candidate.
    """
    messages = [
        {"role": "system", "content": _SYSTEM_PROMPT},
        {"role": "user", "content": _build_user_prompt(expected, actual)},
    ]
    try:
        resp = await provider.chat_with_retry(messages=messages, model=model)
        content = resp.content or ""
    except Exception as e:  # judge is best-effort; never break the eval run
        logger.warning("semantic_quality judge call failed: {}", e)
        return MetricResult(
            name="semantic_quality", score=_NEUTRAL_SCORE, passed=False,
            details={"error": str(e)},
        )

    try:
        raw_score = _parse_score(content)
    except (ValueError, KeyError, TypeError, json.JSONDecodeError):
        logger.warning("semantic_quality could not parse judge reply: {!r}", content[:200])
        return MetricResult(
            name="semantic_quality", score=_NEUTRAL_SCORE, passed=False,
            details={"raw": content[:500]},
        )

    score = max(0.0, min(1.0, raw_score))
    return MetricResult(
        name="semantic_quality", score=score, passed=score >= _PASS_THRESHOLD,
        details={"raw_score": raw_score},
    )
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/test_eval_semantic.py -v`
Expected: PASS（5 passed）

注意：项目用 `python -m pytest`（editable install 路径指向旧位置，直接 `pytest` 会收集失败）。

- [ ] **Step 5: lint**

Run: `ruff check echo_agent/evaluation/semantic_metrics.py tests/test_eval_semantic.py`
Expected: All checks passed

- [ ] **Step 6: 提交**

```bash
git add echo_agent/evaluation/semantic_metrics.py tests/test_eval_semantic.py
git commit -m "新增 LLM-as-judge 语义评测指标 semantic_quality"
```

---

## Task 2: runner.py 接入语义指标

**Files:**
- Modify: `echo_agent/evaluation/runner.py`
- Test: `tests/test_eval_runner.py`

- [ ] **Step 1: 写失败测试**

创建 `tests/test_eval_runner.py`。这些测试只验证「指标按 provider 与 expected_output 的组合正确追加」，用一个直接构造的 `CaseResult` 路径不方便，故测试 `run_case` 的打分段——但 `run_case` 会真正跑 agent。为隔离，改为测试一个抽出的纯打分 helper。先在测试里约定该 helper 存在：

```python
"""Tests for EvalRunner semantic metric wiring."""
from __future__ import annotations

import pytest

from echo_agent.evaluation.runner import EvalRunner
from echo_agent.evaluation.dataset import EvalCase


class _FakeProvider:
    def __init__(self):
        self.calls = 0

    async def chat_with_retry(self, **kwargs):
        self.calls += 1
        class _Resp:
            content = '{"score": 0.8, "reasoning": "ok"}'
        return _Resp()


def _case(expected_output: str = "") -> EvalCase:
    return EvalCase(
        id="t", input="hi", expected_tools=[], expected_contains=[],
        expected_output=expected_output, max_iterations=3, tags=[], metadata={},
    )


@pytest.mark.asyncio
async def test_score_response_no_provider_skips_semantic():
    runner = EvalRunner(agent_loop=None, provider=None)
    metrics = await runner._score_response(_case("some expected text"), "actual", [], 1)
    names = [m.name for m in metrics]
    assert "quality" in names          # response_quality still runs
    assert "semantic_quality" not in names


@pytest.mark.asyncio
async def test_score_response_with_provider_adds_semantic():
    provider = _FakeProvider()
    runner = EvalRunner(agent_loop=None, provider=provider)
    metrics = await runner._score_response(_case("some expected text"), "actual", [], 1)
    names = [m.name for m in metrics]
    assert "quality" in names
    assert "semantic_quality" in names
    assert provider.calls == 1


@pytest.mark.asyncio
async def test_score_response_no_expected_output_skips_both():
    provider = _FakeProvider()
    runner = EvalRunner(agent_loop=None, provider=provider)
    metrics = await runner._score_response(_case(""), "actual", [], 1)
    names = [m.name for m in metrics]
    assert "quality" not in names
    assert "semantic_quality" not in names
    assert provider.calls == 0
```

- [ ] **Step 2: 运行测试确认失败**

Run: `python -m pytest tests/test_eval_runner.py -v`
Expected: FAIL — `EvalRunner.__init__` 不接受 `provider`（TypeError），且无 `_score_response` 方法

- [ ] **Step 3: 改 runner.py**

(a) 顶部 import 段（runner.py:13-16）后追加语义指标 import：

```python
from echo_agent.evaluation.metrics import (
    MetricResult, contains_all, tool_usage_correctness,
    iteration_efficiency, response_quality,
)
from echo_agent.evaluation.semantic_metrics import semantic_quality
```

(b) `EvalRunner.__init__`（runner.py:68）改为接受 provider/judge_model：

```python
    def __init__(
        self,
        agent_loop: AgentLoop,
        parallel: int = 3,
        timeout: int = 120,
        provider: "LLMProvider | None" = None,
        judge_model: str | None = None,
    ):
        self._loop = agent_loop
        self._parallel = parallel
        self._timeout = timeout
        self._provider = provider
        self._judge_model = judge_model
```

并在 TYPE_CHECKING 块（runner.py:18-19）补类型：

```python
if TYPE_CHECKING:
    from echo_agent.agent.loop import AgentLoop
    from echo_agent.models.provider import LLMProvider
```

(c) 把 run_case 里现有的打分段（runner.py:113-118）抽成一个 async helper，并让 run_case 调用它。将这几行：

```python
        # Score
        result.metrics.append(contains_all(case.expected_contains, result.response))
        result.metrics.append(tool_usage_correctness(case.expected_tools, result.tools_used))
        result.metrics.append(iteration_efficiency(result.iterations, case.max_iterations))
        if case.expected_output:
            result.metrics.append(response_quality(case.expected_output, result.response))
        result.passed = all(m.passed for m in result.metrics) and not result.error
        return result
```

替换为：

```python
        # Score
        result.metrics = await self._score_response(
            case, result.response, result.tools_used, result.iterations,
        )
        result.passed = all(m.passed for m in result.metrics) and not result.error
        return result

    async def _score_response(
        self, case: EvalCase, response: str, tools_used: list[str], iterations: int,
    ) -> list[MetricResult]:
        metrics: list[MetricResult] = [
            contains_all(case.expected_contains, response),
            tool_usage_correctness(case.expected_tools, tools_used),
            iteration_efficiency(iterations, case.max_iterations),
        ]
        if case.expected_output:
            metrics.append(response_quality(case.expected_output, response))
        if case.expected_output and self._provider is not None:
            metrics.append(
                await semantic_quality(
                    case.expected_output, response,
                    self._provider, model=self._judge_model,
                )
            )
        return metrics
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest tests/test_eval_runner.py -v`
Expected: PASS（3 passed）

- [ ] **Step 5: lint**

Run: `ruff check echo_agent/evaluation/runner.py tests/test_eval_runner.py`
Expected: All checks passed

- [ ] **Step 6: 提交**

```bash
git add echo_agent/evaluation/runner.py tests/test_eval_runner.py
git commit -m "EvalRunner 接入语义指标，按 provider 与 expected_output 条件激活"
```

---

## Task 3: app.py 注入 provider + baseline 数据补充

**Files:**
- Modify: `echo_agent/app.py:161-166`
- Modify: `data/eval/baseline.yaml`

- [ ] **Step 1: app.py 工厂注入 provider**

`echo_agent/app.py` 的 `_make_eval_runner`（161-166 行）改为：

```python
            def _make_eval_runner() -> EvalRunner:
                return EvalRunner(
                    agent,
                    parallel=config.evolution.eval_parallel,
                    timeout=config.evolution.eval_timeout_seconds,
                    provider=provider,
                )
```

`provider` 已在该闭包作用域内（同函数 app.py:179 已使用），无需新增传参。`judge_model` 留默认 `None`，由 provider 路由用默认推理模型（符合 spec 决策 3「复用主推理模型」）。

- [ ] **Step 2: baseline.yaml 补 expected_output**

`data/eval/baseline.yaml` 中，给 `explain_evolution` case（20-24 行）加一行 `expected_output`：

```yaml
  - id: explain_evolution
    input: "Briefly explain what a self-evolving agent is, in one sentence."
    expected_contains: ["agent"]
    expected_output: "A self-evolving agent is an AI system that improves its own skills or behavior over time based on experience or feedback."
    max_iterations: 3
    tags: [chat]
```

给 `ask_for_help` case（44-48 行）加：

```yaml
  - id: ask_for_help
    input: "How can I check the agent's current status from the CLI?"
    expected_contains: ["status"]
    expected_output: "You can check the agent's current status by running the echo-agent status command in the CLI."
    max_iterations: 4
    tags: [chat, docs]
```

给 `tool_choice_reasoning` case（50-54 行）加：

```yaml
  - id: tool_choice_reasoning
    input: "I want to summarize this text: 'Echo Agent is a modular AI agent framework.' What's the most efficient way to handle a single short sentence?"
    expected_contains: []
    expected_output: "For a single short sentence, no summarization tool is needed — the text is already concise and can be used as-is."
    max_iterations: 4
    tags: [reasoning, tool_use]
```

其余 4 个 case（chat_smoke / list_skills / refuse_nonsense / numeric_reasoning）不加。

- [ ] **Step 3: 验证 YAML 可解析且字段就位**

Run:
```bash
python -c "from echo_agent.evaluation.dataset import EvalDataset; from pathlib import Path; d=EvalDataset.from_path(Path('data/eval/baseline.yaml')); n=[c.id for c in d.cases if c.expected_output]; print(sorted(n))"
```
Expected: `['ask_for_help', 'explain_evolution', 'tool_choice_reasoning']`

- [ ] **Step 4: lint**

Run: `ruff check echo_agent/app.py`
Expected: All checks passed

- [ ] **Step 5: 提交**

```bash
git add echo_agent/app.py data/eval/baseline.yaml
git commit -m "评测工厂注入 provider，baseline 补充 3 个语义评测参考答案"
```

---

## Task 4: 全量回归与 lint

**Files:** 无改动，仅验证。

- [ ] **Step 1: 全量测试**

Run: `python -m pytest -q`
Expected: 全绿，无回归。重点关注 `test_eval_semantic.py`、`test_eval_runner.py`、`test_planning_a2a_eval.py`（现有唯一 eval 相关测试）。

注意：必须用 `python -m pytest`（不是 `pytest`）——editable install 路径指向旧位置，直接调用会收集失败。

- [ ] **Step 2: lint 全量**

Run: `ruff check .`
Expected: All checks passed

- [ ] **Step 3: 若有修复则提交**

```bash
git add -A
git commit -m "WP-B 回归修复与 lint"
```

（无修复则跳过。）

---

## 验证完成定义（对照 spec 第四节）

- [ ] semantic_quality：高分 pass / 低分 fail / 解析失败中性 0.5 / 异常中性 0.5 / 越界 clamp
- [ ] runner：provider=None 跳过语义指标；provider 非空且有 expected_output 时追加；无 expected_output 时两个 quality 指标都不加
- [ ] app.py 工厂传入 provider；baseline 3 个 case 有 expected_output
- [ ] 全量 `python -m pytest` 无回归；`ruff check .` 通过
- [ ] 现有无 expected_output 的 case 打分不变（历史基准零影响）

---

## 自查记录

- **Spec 覆盖**：决策1（新增独立指标）→ Task 1；决策2（纳入 avg_score）→ Task 2 的 `_score_response` 把指标加进 `result.metrics`，沿用现有均值机制，无需额外代码；决策3（复用主推理模型）→ Task 3 judge_model=None；决策4（独立文件保 metrics.py 纯净）→ Task 1 新建 semantic_metrics.py。数据补充 → Task 3。测试 → 各 Task + Task 4。
- **偏差**：spec 列的 gate.py 改动经核实不需要（工厂闭包已持有 provider），已在顶部说明，文件清单据此调整。
- **占位符扫描**：无 TBD/TODO；每个改码步含完整代码。
- **类型一致性**：`semantic_quality(expected, actual, provider, *, model=None)` 在 Task 1 定义、Task 2 调用一致；`MetricResult(name="semantic_quality", ...)` 字段与 metrics.py:9-14 定义一致；`_score_response` 在 Task 2 定义并被同任务测试调用，签名一致；`EvalRunner(provider=...)` 在 Task 2 定义、Task 3 使用一致。
- **测试框架**：用了 `@pytest.mark.asyncio`——已确认项目测试中使用 async 测试（test_plugin_manager 用 asyncio.run，但 pytest-asyncio 在 [dev] 依赖中；若收集报 "async def not natively supported"，改用 `asyncio.run()` 包裹，或确认 pyproject 的 asyncio_mode）。Task 4 Step 1 会暴露此问题，届时按提示调整。
